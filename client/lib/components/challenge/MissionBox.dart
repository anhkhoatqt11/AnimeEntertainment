import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MisssionBox extends StatelessWidget {
  const MisssionBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}